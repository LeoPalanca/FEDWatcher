server {
    server_name fedwatcher.ellep.it;

    root /var/www/fedwatcher;
    index index.html;

    access_log /var/log/nginx/fedwatcher.access.log;
    error_log /var/log/nginx/fedwatcher.error.log;

    location / {
        try_files $uri $uri/ =404;
    }

    listen [::]:443 ssl; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/fedwatcher.ellep.it/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/fedwatcher.ellep.it/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = fedwatcher.ellep.it) {
        return 301 https://$host$request_uri;
    } # managed by Certbot

    listen 80;
    listen [::]:80;
    server_name fedwatcher.ellep.it;
    return 404; # managed by Certbot
}
